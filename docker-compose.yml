version: "3.9"

services:
  simple-proxy:
    build:
      context: /tmp/empty
      dockerfile: ${CWD:-PWD}/Dockerfile.pk-proxy-image   # change if you named it differently
    container_name: pk-proxy-image
    ports:
      - "3000:${PK-PROXY-PORT:-3222}"
    restart: unless-stopped
